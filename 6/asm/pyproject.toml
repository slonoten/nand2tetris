[project]
name = "asm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.1.1",
]

[tool.uv]
package = true  # Forces uv to install your local code into the venv 
