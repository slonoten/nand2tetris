[project]
name = "vm-translator"
version = "0.1.0"
description = "VM Translator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lark>=1.3.1",
    "pytest>=9.1.1",
]

[tool.uv]
package = true  # Forces uv to install your local code into the venv 
